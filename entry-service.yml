apiVersion: apps/v1
kind: Deployment
metadata:
  name: entry-service
spec:
  selector:
    matchLabels:
      app: entry-service
  replicas: 1
  template:
    metadata:
      labels:
        app: entry-service
    spec:
      containers:
      - name: entry-service
        image: berly01/diary-entry-service:latest
        ports:
        - containerPort: 5002
        env:
        - name: DB_HOST
          value: mysql
        - name: DB_USER
          value: root
        - name: DB_PASSWORD
          value: rootpassword
        - name: DB_NAME
          value: diary_db
        - name: SECRET_KEY
          value: tu_clave_secreta_super_segura_123
---
apiVersion: v1
kind: Service
metadata:
  name: entry-service
spec:
  type: ClusterIP
  selector:
    app: entry-service
  ports:
    - port: 5002
      targetPort: 5002
